/** SocketIO client stub. */
export class RealtimeClient {
  async connect(): Promise<void> {
    throw new Error("RealtimeClient.connect — not implemented (Phase 2)");
  }

  async disconnect(): Promise<void> {}
}
